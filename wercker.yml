box: alpine

build:
  steps:
    - shellcheck:
      files: run.sh

    - script:
      name: prepare output
      code: rm -Rf $WERCKER_ROOT/.git

publish:
  steps:
    - internal/publish-step:
      owner: brett
